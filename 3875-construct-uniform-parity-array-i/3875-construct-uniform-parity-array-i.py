from typing import List

class Solution:
    def uniformArray(self, nums1: List[int]) -> bool:
        # As proven by parity combinations, a valid uniform array 
        # can mathematically always be constructed for any input.
        return True